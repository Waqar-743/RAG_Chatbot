# ============================================================
# RAG Chatbot — Production Requirements (Vercel-compatible)
# 
# llama-index is intentionally excluded: it is 200 MB+ and
# would exceed Vercel's 250 MB serverless limit.
# The RAG pipeline uses qdrant-client + openai directly.
#
# For local dev / Docker: use requirements-full.txt instead
# ============================================================

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
mangum==0.17.0

# Config
python-dotenv==1.0.1
pydantic==2.9.1
pydantic-settings==2.5.2

# OpenAI / OpenRouter client (embeddings + LLM)
openai==1.45.0
httpx==0.27.2

# Vector Database
qdrant-client==1.11.3

# MongoDB
pymongo==4.8.0
motor==3.5.1

# Async HTTP / scraping
aiohttp==3.10.5
beautifulsoup4==4.12.3

# File Processing
pypdf==4.3.1
python-docx==1.1.2
docx2txt==0.9
tiktoken==0.12.0
langchain-text-splitters

# Reliability
tenacity==8.2.3

# Numerics
numpy>=1.26,<3

# Logging
structlog==24.4.0

# Testing (needed for CI)
pytest==8.3.3
pytest-asyncio==0.24.0
