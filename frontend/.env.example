# Frontend environment variables
# Copy to .env.local for local dev (or set in Vercel dashboard for prod).
# Leave blank in dev — Vite proxies /api → http://localhost:8000.

VITE_API_URL=https://ragnarok-api.onrender.com
